{
  "name": "vollcrypt",
  "version": "0.1.0",
  "description": "Cross-platform Rust cryptography core for VollChat & KanzleiLink",
  "private": true,
  "scripts": {
    "build:node": "cd node && npx @napi-rs/cli build --platform --release",
    "build:wasm": "npx wasm-pack build wasm --target web --out-dir pkg",
    "build": "npm run build:node && npm run build:wasm",
    "test:cargo": "cargo test --all",
    "format": "cargo fmt --all"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.18.4",
    "@types/node": "^25.3.0",
    "wasm-pack": "^0.0.0"
  }
}
