{
  "name": "vollcrypt",
  "version": "0.2.0",
  "description": "Cross-platform Rust cryptography core for VollChat & KanzleiLink",
  "private": true,
  "scripts": {
    "build:node": "cd vollcrypt-messages/node && npm install && npm run build",
    "build:wasm": "wasm-pack build vollcrypt-messages/wasm --target web --out-dir pkg",
    "build": "npm run build:node && npm run build:wasm",
    "test:cargo": "cargo test --all",
    "format": "cargo fmt --all"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.18.4",
    "@types/node": "^25.3.0",
    "wasm-pack": "^0.0.0"
  }
}
