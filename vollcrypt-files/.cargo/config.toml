# Default to portable target-cpu for distribution.
# For maximum performance during local benchmarks, run with RUSTFLAGS="-C target-cpu=native"
[target.'cfg(target_arch = "x86_64")']
rustflags = ["-C", "target-cpu=x86-64-v3"]
