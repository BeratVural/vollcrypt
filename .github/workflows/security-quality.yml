name: Security and Quality Gates

on:
  push:
    branches: ["main"]
    paths:
      - "**/*.rs"
      - "**/*.ts"
      - "**/*.js"
      - "Cargo.toml"
      - "Cargo.lock"
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/security-quality.yml"
  pull_request:
    branches: ["main"]

permissions:
  contents: read
  security-events: write

jobs:
  rust-quality:
    name: Rust fmt, clippy, and audit
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Check formatting
        run: cargo fmt --all --check

      - name: Run clippy as errors
        run: cargo clippy -p vollcrypt-core -p vollcrypt-files-core -p vollcrypt-db-guard --all-targets -- -D warnings

      - name: Install cargo-audit
        uses: taiki-e/install-action@cargo-audit

      - name: Audit Rust dependencies
        run: cargo audit

  node-audit:
    name: Node dependency audit
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Install Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Rust for SBOM generation
        uses: dtolnay/rust-toolchain@stable

      - name: Audit npm workspaces
        run: npm audit --workspaces --audit-level=high

      - name: Validate release SBOM generation
        run: |
          node scripts/generate-cargo-sbom.js vollcrypt-files/node/Cargo.toml vollcrypt-files/node/package.json ${{ runner.temp }}/files-node-sbom.json
          node scripts/generate-cargo-sbom.js vollcrypt-files/wasm/Cargo.toml vollcrypt-files/wasm/package.json ${{ runner.temp }}/files-wasm-sbom.json
          node scripts/generate-cargo-sbom.js vollcrypt-messages/node/Cargo.toml vollcrypt-messages/node/package.json ${{ runner.temp }}/messages-node-sbom.json
          node scripts/generate-cargo-sbom.js vollcrypt-messages/wasm/Cargo.toml vollcrypt-messages/wasm/package.json ${{ runner.temp }}/messages-wasm-sbom.json

  codeql:
    name: CodeQL analysis
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: javascript-typescript

      - name: Autobuild
        uses: github/codeql-action/autobuild@v4

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v4
