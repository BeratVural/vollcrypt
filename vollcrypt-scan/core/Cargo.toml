[package]
name = "vollcrypt-scan-core"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-only OR LicenseRef-Commercial"
description = "Product-neutral bounded filesystem scanning core for Vollcrypt analysis tools"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"
walkdir = "2.5"

[dev-dependencies]
tempfile = "3"
